#include "Modules/ModuleManager.h"

// No custom startup/shutdown logic needed yet — the default module
// implementation is enough. Add a real FSaveFrameworkModule class here
// later if you need to register console commands, editor tooling, etc.
IMPLEMENT_MODULE(FDefaultModuleImpl, SaveFramework)
